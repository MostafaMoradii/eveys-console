// Generated types are emitted by `pnpm gen:api-types` into ./generated/.
// They are gitignored — every clone runs the generator as part of `pnpm install`.
// Re-export here so consumers don't depend on the generated path.
export type * from './generated/openapi.js';
